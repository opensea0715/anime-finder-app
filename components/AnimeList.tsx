// This component is being phased out in favor of AnimeSection for the Prime Video style layout.
// It can be removed or repurposed if a simple grid list view is needed elsewhere.
import React from 'react';

const AnimeList: React.FC = () => {
  return null; 
};

export default AnimeList;